<form action="{{ url('admin_post_delete'), {id: post.id} }}" method="post">
    {# the argument of crsf_token() is an arbitrary string used to generate the token #}
    <input type="hidden" name="token" value="{{ csrf_token('delete_item') }}"/>

    <button type="submit"> Delete item </button>
</form>